
const contractor = 'Contractor';
const signer = 'Signer';
const viewer = 'Viewer';
const admin = 'Admin';
const all = [contractor, signer, viewer, admin];
const canSearch = [contractor, signer, viewer];

module.exports = {
    contractor: contractor,
    signer: signer,
    viewer: viewer,
    admin: admin,
    all: all,
    canSearch: canSearch
};